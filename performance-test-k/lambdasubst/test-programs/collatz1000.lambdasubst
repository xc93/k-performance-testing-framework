( lambda bound .
    (( lambda collatz .
       ((lambda iter . (iter bound)) 
        (mu g . lambda n . 
                  (if n = 0 
                   then 0 
                   else ((g (n - 1)) + (collatz n))))))
     (mu f . lambda x . 
               (if x <= 1 then 1
                else (if (x / 2) * 2 = x then (1 + (f (x / 2)))
                else (1 + (f (3 * x + 1)))))))) 
1000

